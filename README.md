# School System

## Do this:

* run ```build.py```
* run ```example_data.py```
* run ```menu_oop.py```
  * choose from the options listed in the menu
* to view the online registration form, run ```app.py```
